# Trenchy

Railway-ready Trenchy dashboard rebuilt without the incompatible Solana/rpc-websockets/uuid dependency chain.

## Deploy to Railway
1. Upload this project with `package.json` at the root.
2. Railway will install dependencies automatically.
3. Start command: `npm start`
4. Health check: `/health`

Do not manually set `PORT`; Railway supplies it.
